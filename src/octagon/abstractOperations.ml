(* Define basic operations in the abstract domain *)
open Types

(* The abstraction function maps from a program state to a DBM. *)
let alpha state : DBM =

let join a1 a2 : DBM =

let meet a1 a2 : DBM =